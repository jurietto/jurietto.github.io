// Example Firebase config file — DO NOT COMMIT your real keys.
// Copy this file to `js/firebase-config.js` and fill in real values.
// The file should run in the browser and assign the config to a global:
//
//   window.__FIREBASE_CONFIG__ = { ... }
//
// This keeps secrets out of the repository. For CI/CD, inject the real
// config at build-time (GitHub Actions, Cloudflare Worker, etc.).

window.__FIREBASE_CONFIG__ = {
  apiKey: "REPLACE_WITH_FIREBASE_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
};
